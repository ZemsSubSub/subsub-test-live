# Хендофф между сессиями / компьютерами

Читать этот файл ПЕРВЫМ при возобновлении работы (новый компьютер, новая сессия, потеря контекста). Остальные `0X-*.md` в этой папке — источники правил, этот файл — снимок "где мы сейчас" на 2026-07-15.

## Модель работы (важно понимать, иначе прошлые решения не имеют смысла)

Работа идёт в ДВУХ параллельных диалогах с Claude:

1. **Этот диалог (Cowork, Sonnet)** — не пишет код напрямую в прототип. Здесь пользователь показывает скриншоты/фидбек по тому, что сделал Клод Код, а я формулирую точные промпты-инструкции для другого диалога.
2. **Второй диалог ("Клод Код"/Opus)** — реально пишет файлы в `E:\Dev SubSub\subsub_front_prototype_14.07.26\`, следуя `instructions/01-05` и промптам, которые я готовлю в этом диалоге.

Если продолжаешь работу в НОВОМ диалоге — уточни у пользователя, это диалог "промпт-мейкера" (как этот) или диалог "исполнителя" (как Клод Код), поведение должно соответствовать роли.

## Папки

- `E:\Dev SubSub\subsub_platform_front` — настоящий репозиторий (Nuxt 3, монорепо). **Только читать**, ничего не писать. Источник i18n-текстов, дизайн-токенов, реальной структуры компонентов. НЕ источник истины для визуала — Storybook там устарел.
- `E:\Dev SubSub\subsub_front_prototype_14.07.26` — рабочая папка прототипа. Сюда пишет Клод Код. `instructions/` внутри неё — правила и трекер.
- Главный источник истины по внешнему виду — живой прод **https://app.subsub.io** (проверять через Claude in Chrome/DevTools), а не репозиторий и не Storybook.

## Что уже сделано (подтверждено пользователем)

- Инфраструктура (`css/tokens.css`, `css/styles.css`, иконки, картинки) — готова.
- **Home** (`home.html`) — собрана, визуально сверена с продом, есть на подтверждении у пользователя.
- **Сайдбар/навигация** — механика исправлена:
  - Пункты левого сайдбара (кроме Home) — radio-группа (`name="spaces-menu-type"`), клик НЕ навигация, а открытие второй (саб-)панели. Активность — `:has(input:checked)`, inset-тень слева, НЕ фон.
  - Показ/скрытие саб-панели — через `js/nav.js`.
  - Collapse/expand кнопка саб-панели реализована; последние правки координат применены:
    - `.toggle--expand { top: 131px; left: 57px; display: flex; }`
    - `.toggle--collapse { top: 58px; right: -17px; }`
  - Саб-панели всех 8 разделов (Analytics, Fan Funding, Wallet, Network, Live, Media Library, Content Export, Admin) наполняются реальными пунктами-ссылками (собранными с прода), но ссылки — заглушки (`href="#"`), т.к. сами страницы разделов ещё не собраны. Этот шаг был отправлен Клод Коду последним промптом — **результат ещё не подтверждён пользователем на момент записи этого файла**.

## Изменение приоритета страниц (актуально, отменяет более старые пункты в 05-progress.md)

Изначальный план — Home → Live (Overview → Streams) → остальные разделы по порядку. **Пользователь изменил порядок**: следующая полноценная страница — **Media Library → Files**, а не Live.

Важные уточнения по Media Library:
- Строим ТОЛЬКО страницу `media-library/files` (список файлов). Страницу `media-library/overview` (лендинг/калькулятор) — **не делаем вообще**, сознательно пропускаем.
- Реальная структура страницы Files (сверено на живом проде): заголовок "Media Library", кнопки "Upload"/"Create folder", поиск, табы "All media/My media/Shared media", таблица с колонками NAME | CREATED | STORAGE | AUTHOR | SHARED WITH | (действия).
- Превью файла — блок 70×48, реальный thumbnail либо иконка по типу (`FILE_CATEGORY` из `layers/core/utils/fileCategory.ts`: video/image/audio/document/spreadsheet/pdf/code/other).
- **Ключевое требование пользователя**: на проде реально загружены только видео/PDF/папка (остальные типы пользователь туда не грузил) — но в ПРОТОТИПЕ нужно показать ВСЕ типы файлов из `FILE_CATEGORY` с их превью/иконками, даже те, которых нет на проде. Это черновой первый проход по превью — дальше их будут донастраивать/поправлять отдельно.
- Полный текст промпта с этой задачей отправлен пользователем в Клод Код — см. историю переписки этого диалога или спроси пользователя, применил ли он его.

## Что дальше (после подтверждения Media Library → Files)

По изначальному плану после реприоритизации остаются несобранными (порядок между ними пока не переопределён, кроме того что Media Library теперь раньше Live):
- Live → Overview
- Live → Streams
- Analytics (страница, не саб-меню — саб-меню уже наполнено)
- Fan Funding
- Wallet
- Network
- Content Export
- Admin

Уточнить у пользователя актуальный порядок перед стартом каждой следующей страницы — приоритеты уже менялись один раз и могут поменяться снова.

## Рабочий процесс на будущее

Для каждой страницы: Клод Код собирает → пользователь смотрит скриншот/локально → если есть правки, пользователь описывает их (часто в этом, "промпт-мейкерском" диалоге) → я формулирую один цельный промпт → пользователь копирует его в диалог с Клод Кодом → повтор до подтверждения → обновить `05-progress.md`.
